interface:
  display_name: "Pluto"
  short_description: "Route requests to Pluto's live recruiting tools"
  default_prompt: "Use Pluto's currently available recruiting tools for this request."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
