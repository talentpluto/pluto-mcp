interface:
  display_name: "Pluto credit balance"
  short_description: "Check shared organization credits"
  default_prompt: "Use $credit-balance to check my organization's shared monthly Pluto credits and reset date."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
