interface:
  display_name: "Pluto connection recovery"
  short_description: "Recover Pluto connections and resume existing operations"
  default_prompt: "Use $connection-recovery to diagnose Pluto, use my host's sign-in route when authentication is required, verify a read, and resume my existing operation."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
