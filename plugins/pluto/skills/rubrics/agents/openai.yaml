interface:
  display_name: "Pluto candidate rubrics"
  short_description: "Create or update candidate rubrics"
  default_prompt: "Use $rubrics to draft or edit one complete candidate rubric, show the full proposed contents for review, and save only after I confirm the exact proposal."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
