interface:
  display_name: "Pluto candidate discovery"
  short_description: "Run evidence-qualified candidate searches"
  default_prompt: "Use $candidate-discovery to forward my complete safe professional query or raw JD once, supply a fresh request ID, report the server's effective search interpretation, and present all four returned result groups with their exact evidence and credit accounting, using any returned Client preference fit reason as the primary Why this person source."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
