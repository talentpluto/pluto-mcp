interface:
  display_name: "Pluto candidate discovery"
  short_description: "Run evidence-qualified candidate searches"
  default_prompt: "Use $candidate-discovery to forward my complete safe professional search once, supply a fresh request ID, and present all four returned result groups with their exact evidence and credit accounting."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
