interface:
  display_name: "Pluto candidate discovery"
  short_description: "Run open-world candidate searches with Pluto"
  default_prompt: "Use $candidate-discovery to forward my complete safe professional search request once and assess every result with its returned evidence gaps."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
