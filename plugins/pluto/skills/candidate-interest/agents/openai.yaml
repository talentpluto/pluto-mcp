interface:
  display_name: "Pluto candidate interest"
  short_description: "Act on one candidate or enrich external emails"
  default_prompt: "Use $candidate-interest for the candidates I explicitly selected from my Pluto results. Route one in-network interest action separately from a one-to-100-candidate external email batch, preserve every handle pair and input order, return each committed email with its separate verification outcome, and retain each fresh successful handle privately for a later draft or campaign."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
