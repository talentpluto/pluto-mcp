interface:
  display_name: "Pluto candidate interest"
  short_description: "Act on an explicitly selected candidate"
  default_prompt: "Use $candidate-interest to express interest in the candidate I explicitly selected from my Pluto results."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
