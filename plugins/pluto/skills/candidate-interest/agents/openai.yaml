interface:
  display_name: "Pluto candidate interest"
  short_description: "Act on or enrich a selected candidate"
  default_prompt: "Use $candidate-interest for the candidate I explicitly selected from my Pluto results, routing internal interest and out-of-network email enrichment safely."

dependencies:
  tools:
    - type: "mcp"
      value: "pluto"
      description: "Pluto's authenticated TalentPluto recruiting MCP server"
      transport: "streamable_http"
      url: "https://app.talentpluto.com/api/mcp"
